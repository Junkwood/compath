<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.ProjectMapperJDJ">
    <!-- 프로젝트 전체조회 -->
    <select id="getAllProject" resultType="ProjectDTOJDJ">
        SELECT *
        FROM PROJECTS
    </select>

    <!-- 프로젝트 생성 -->
    <insert id="registerProject">
        INSERT INTO PROJECTS VALUES(
        PROJECT_ID,
        PARENT_PROJECT_ID,
        PROJECT_NAME,
        DESCRIPTION,
        START_DATE,
        END_DATE,
        STATUS,
        IS_PUBLIC,
        CREATED_AT,
        UPDATED_AT,
        CREATED_BY,
        PM_USER_ID,
        PL_USER_ID,
        EDITOR_USER_ID )
    </insert>


</mapper>