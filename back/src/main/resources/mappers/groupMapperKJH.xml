<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.group.mapper.GroupMapperKJH">

    <select id="getAllgroupMembers">
        SELECT g.user_id, u.user_name, g.group_id
        FROM group_members g
        JOIN users u ON g.user_id = u.user_id
        WHERE g.group_id = #{id}
        GROUP BY g.user_id, u.user_name, g.group_id
    </select>
</mapper>