<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.group.mapper.GroupMapperKJH">

    <select id="getAllgroupMembers">
        SELECT u.user_id, u.user_name, gm.group_id
        FROM group_members gm
        JOIN groups g ON gm.group_id = g.group_id
        JOIN users u ON gm.user_id = u.user_id
        <if test="id != null">
        WHERE g.group_id = #{id} AND g.group_type = 'C1'
        </if>
        <if test="name != null">
            WHERE u.user_name = #{name} AND g.group_type = 'C1'
        </if>
        ORDER BY gm.group_id
    </select>
</mapper>