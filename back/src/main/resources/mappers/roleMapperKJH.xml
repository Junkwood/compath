<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.role.mapper.RoleMapperKJH">

<!--    공지사항/문서 생성시 카테고리 -->
    <select id="getNoticeRole">
        SELECT DISTINCT(r.role_id), r.role_name
        FROM project_member_roles p
        JOIN project_members p2 ON p.project_member_id = p2.project_member_id
        JOIN roles r ON r.role_id = p.role_id
        WHERE p2.project_id = #{id} AND r.is_active = 'O1'
    </select>

<!--    프로젝트 내 권한 구분-->
    <select id="getProjectRole">
        SELECT pm.user_id
        FROM project_members pm
        LEFT JOIN project_member_roles pmr ON pm.project_member_id = pmr.project_member_id
        WHERE pm.project_id=#{projectId}
        <if test="subProjectId != null and subProjectId != 0" >
            OR pm.project_id=#{subProjectId}
        </if>
        AND pm.is_active = 'O1'
        AND (pmr.role_id = 1 OR pmr.role_id = 3)
    </select>
</mapper>