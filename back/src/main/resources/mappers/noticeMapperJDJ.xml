<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.notice.mapper.NoticeMapperJDJ">

<select id="getNoticeListByPid" resultType="NoticeDtoJDJ">
    SELECT
        NOTICE_ID,
        TITLE,
        CREATED_AT,
        IS_PINNED
    FROM NOTICES
    WHERE PROJECT_ID=#{projectId}
        AND IS_DELETED='Q2'
    ORDER BY IS_PINNED, CREATED_AT
</select>

</mapper>